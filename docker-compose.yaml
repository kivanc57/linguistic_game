version: '3.8'

services:
  ling_game:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
    - ./src:/home/linguistic_game/src
    - ./config:/home/linguistic_game/config
    - ./resources:/home/linguistic_game/resources
    command: ["python", "src/main.py"]
